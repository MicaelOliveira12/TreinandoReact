import styled from 'styled-components';

function Title(){
    
    const Title = styled.h1`
    width:100%
    margin: 0;
    padding: 0;
    text-align: center;
    font-size: 2em;
    `
    return(

        <Title>PRODUTOS</Title>
    );
}

export default Title;
